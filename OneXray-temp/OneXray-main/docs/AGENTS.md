# 文档维护规则

1. 本目录只记录当前产品行为、架构边界和仍然有效的工程约束。已完成的实施计划、阶段记录和一次性调研结论交给 Git 历史保存。
2. 修改文档前先核对对应代码和测试。容易从源码、配置或工作流直接读取的字段清单、依赖版本和行号不在文档中重复维护。
3. 每个主题只保留一个事实来源；需求变化时更新原文档，不新增并列或互相覆盖的说明。
4. 正文使用中文，代码标识、协议名、平台 API 和用户界面中的固定英文名称可以保留原文。
5. 文件名使用 kebab-case。新增链接时使用仓库相对路径，并在提交前检查失效链接和 `git diff --check`。
